{
  "name": "backlog-jira-sync-system",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "System for backlog management and Jira sync via MCP",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run -w @task-project/backend dev\" \"npm run -w @task-project/frontend dev\"",
    "build": "npm run -w @task-project/backend build && npm run -w @task-project/frontend build",
    "test": "npm run -w @task-project/backend test",
    "typecheck": "npm run -w @task-project/backend typecheck && npm run -w @task-project/frontend typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "dotenv": "^17.4.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "typescript": "^5.0.0"
  }
}
